name: Publish

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish, e.g. 0.1.2"
        required: true
        type: string

permissions:
  contents: write
  id-token: write

concurrency:
  group: npm-publish-${{ github.event.inputs.version }}
  cancel-in-progress: false

jobs:
  publish:
    name: publish
    runs-on: ubuntu-latest
    environment: npm-publish

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Install latest npm with trusted publishing support
        run: npm install --global npm@latest

      - name: Show toolchain
        run: |
          node --version
          npm --version

      - name: Install dependencies
        # The lockfile records registry tarball URLs from registry.npmmirror.com.
        # npm 11 defaults allow-remote=none, so allow committed lockfile URLs
        # during install while publish still targets registry.npmjs.org below.
        run: npm ci --allow-remote=all

      - name: Verify workspace
        run: npm run verify

      - name: Check package versions
        run: |
          set -euo pipefail
          VERSION="${{ github.event.inputs.version }}"
          for path in \
            packages/pi-better-background-tasks/package.json \
            packages/pi-better-subagents/package.json \
            packages/pi-better-goal/package.json \
            packages/pi-better-harness/package.json
          do
            node -e "const p=require('./$path'); if (p.version !== '$VERSION') throw new Error('$path has '+p.version+', expected $VERSION')"
          done

      - name: Check published versions
        run: |
          set -euo pipefail
          VERSION="${{ github.event.inputs.version }}"
          for pkg in \
            pi-better-background-tasks \
            pi-better-subagents \
            pi-better-goal \
            pi-better-harness
          do
            if npm view "${pkg}@${VERSION}" version --registry=https://registry.npmjs.org/ >/dev/null 2>&1; then
              echo "${pkg}@${VERSION} is already published; this workflow will skip it."
            else
              echo "${pkg}@${VERSION} is not published yet."
            fi
          done

      - name: Pack dry run
        run: |
          set -euo pipefail

          check_pack_file() {
            local file="$1"
            local required_path="$2"
            node -e 'const data=require(process.argv[1]); const p=Array.isArray(data) ? data[0] : Object.values(data)[0]; if (!p?.files?.some((f) => f.path === process.argv[2])) throw new Error(`missing ${process.argv[2]}`);' "$file" "$required_path"
          }

          npm pack --dry-run -w packages/pi-better-background-tasks --json --registry=https://registry.npmjs.org/ > /tmp/pi-bg-pack.json
          check_pack_file /tmp/pi-bg-pack.json src/shared-navigator.ts

          npm pack --dry-run -w packages/pi-better-subagents --json --registry=https://registry.npmjs.org/ > /tmp/pi-subagents-pack.json
          check_pack_file /tmp/pi-subagents-pack.json shared-navigator.ts

          npm pack --dry-run -w packages/pi-better-goal --json --registry=https://registry.npmjs.org/ > /tmp/pi-goal-pack.json
          npm pack --dry-run -w packages/pi-better-harness --json --registry=https://registry.npmjs.org/ > /tmp/pi-harness-pack.json
          check_pack_file /tmp/pi-harness-pack.json node_modules/pi-better-subagents/index.ts
          check_pack_file /tmp/pi-harness-pack.json node_modules/pi-better-background-tasks/src/index.ts
          check_pack_file /tmp/pi-harness-pack.json node_modules/pi-better-goal/src/index.ts

      - name: Publish packages
        run: |
          set -euo pipefail
          VERSION="${{ github.event.inputs.version }}"

          publish_workspace() {
            local pkg="$1"
            local workspace="$2"
            if npm view "${pkg}@${VERSION}" version --registry=https://registry.npmjs.org/ >/dev/null 2>&1; then
              echo "Skipping ${pkg}@${VERSION}; already published."
              return
            fi
            npm publish -w "$workspace" --registry=https://registry.npmjs.org/
          }

          publish_workspace pi-better-background-tasks packages/pi-better-background-tasks
          publish_workspace pi-better-subagents packages/pi-better-subagents
          publish_workspace pi-better-goal packages/pi-better-goal
          publish_workspace pi-better-harness packages/pi-better-harness

      - name: Verify npm versions
        run: |
          set -euo pipefail
          VERSION="${{ github.event.inputs.version }}"
          verify_package() {
            local pkg="$1"
            for attempt in {1..12}; do
              if npm view "${pkg}@${VERSION}" version --registry=https://registry.npmjs.org/ >/tmp/npm-version.txt 2>/tmp/npm-version.err; then
                actual="$(cat /tmp/npm-version.txt)"
                if [ "$actual" = "$VERSION" ]; then
                  echo "$pkg@$actual"
                  return
                fi
              fi
              echo "Waiting for ${pkg}@${VERSION} to be visible on npm (attempt ${attempt}/12)."
              sleep 10
            done
            cat /tmp/npm-version.err >&2 || true
            echo "${pkg}@${VERSION} did not become visible on npm." >&2
            exit 1
          }

          for pkg in \
            pi-better-background-tasks \
            pi-better-subagents \
            pi-better-goal \
            pi-better-harness
          do
            verify_package "$pkg"
          done

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          VERSION="${{ github.event.inputs.version }}"
          set +e
          git ls-remote --exit-code --tags origin "refs/tags/v${VERSION}" >/tmp/release-tag.txt
          TAG_STATUS=$?
          set -e
          case "$TAG_STATUS" in
            0)
              git fetch origin "refs/tags/v${VERSION}:refs/tags/v${VERSION}"
              TAG_SHA="$(git rev-list -n 1 "v${VERSION}")"
              if [ "$TAG_SHA" != "$GITHUB_SHA" ]; then
                echo "v${VERSION} already points to ${TAG_SHA}, expected ${GITHUB_SHA}." >&2
                exit 1
              fi
              ;;
            2)
              git config user.name "github-actions[bot]"
              git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
              git tag -a "v${VERSION}" "$GITHUB_SHA" -m "Release v${VERSION}"
              git push origin "refs/tags/v${VERSION}"
              ;;
            *)
              echo "Could not determine whether v${VERSION} already exists." >&2
              exit "$TAG_STATUS"
              ;;
          esac

          if gh release view "v${VERSION}" >/dev/null 2>&1; then
            echo "Release v${VERSION} already exists at the verified tag."
            exit 0
          fi

          awk -v version="$VERSION" '
            $0 ~ "^## \\[" version "\\]" { in_release = 1; next }
            in_release && /^## / { exit }
            in_release { print }
          ' CHANGELOG.md > /tmp/release-notes.md
          if ! grep -q '[^[:space:]]' /tmp/release-notes.md; then
            echo "CHANGELOG.md has no release notes for ${VERSION}." >&2
            exit 1
          fi

          gh release create "v${VERSION}" \
            --verify-tag \
            --title "v${VERSION}" \
            --notes-file /tmp/release-notes.md