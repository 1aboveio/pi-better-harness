{
  "name": "failure-observations",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared durable failure observations independent of task lifecycle.",
  "scripts": {
    "typecheck": "tsc --noEmit --allowImportingTsExtensions --module NodeNext --moduleResolution NodeNext --target ES2022 --types node --strict --skipLibCheck index.ts *.test.ts",
    "test": "node --import tsx --test *.test.ts"
  }
}
