# pi-better-harness

Meta package for the core Pi Better Harness extensions.

## Install

```sh
pi install npm:pi-better-harness
```

This installs and loads:

- `@1aboveio/pi-better-subagents`
- `@1aboveio/pi-better-background-tasks`
- `@1aboveio/pi-better-goal`

`@1aboveio/pi-better-read-aloud` is intentionally not included yet.